image: ros:lunar-desktop-zesty

before_script:
  - pip install numpy sgp4 pytest pdoc matplotlib scikit-learn
  - apt-get -qq update
  - apt-get -qq install software-properties-common python-software-properties -y
  - add-apt-repository ppa:timsc/opencv-3.2
  - apt-get -qq update
  - apt-get -qq install opencv -y
  - PYTHONPATH="${PYTHONPATH}:${CI_PROJECT_DIR}/nodes/space_vision/src/"
  - export PYTHONPATH

space_vision:
  script:
    - pdoc --html --html-dir docs nodes/space_vision/src/vision_algo/
    - cp -r docs public

  artifacts:
    paths:
    - docs/
    - public

