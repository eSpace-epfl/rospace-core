cmake_minimum_required(VERSION 2.8.3)
project(epoch_clock)

find_package(catkin_simple REQUIRED)
catkin_simple(ALL_DEPS_REQUIRED)
catkin_package()
#cs_install()
catkin_python_setup()
if(CATKIN_ENABLE_TESTING)
	find_package(rostest REQUIRED)
	add_rostest(test/testSimTime.test)
endif()
#cs_export()
